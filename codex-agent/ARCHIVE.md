# Codex SDK Archive

The previous Codex SDK implementation is kept in Git history at commit
`802cac9`. It is not duplicated in the current source tree.

The active implementation lives in `agents-sdk-agent/` and is wired from the
root `pyproject.toml`.

The archived version used `openai-codex==0.1.0b3` through the old `codex` extra.
