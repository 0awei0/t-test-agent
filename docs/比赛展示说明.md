# AI 测试官比赛展示说明

## 演示数据边界

比赛入口中的 TAPD 与工蜂 MR 均为**一一对应的合成任务卡**，用于让评委快速体验“选择任务 → 生成测试计划 → 执行验证 → 输出发布建议”的交互流程。

- 页面不会请求模拟链接，不会读取真实 TAPD、工蜂或业务仓库数据。
- 演示场景只在本地隔离工作区生成合成订单、优惠、库存和支付重试数据。
- 导出的页面只包含脱敏后的结论、事件摘要和合成截图；不会包含 token、真实业务数据、完整日志或本地绝对路径。

## 本地实际运行合成测试

EdgeOne 页面会按真实事件顺序动态复盘一次脱敏运行，但不会在静态站点保存模型密钥或启动测试。需要从网页点击实际执行时，在仓库根目录启动本地任务工作台：

```bash
uv run ai-test-officer dashboard \
  --runs-root runs/live-runs \
  --host 127.0.0.1 \
  --port 8789
```

打开 `http://127.0.0.1:8789/`，选择任务、生成结构化计划，再点击“开始实际执行”。页面会自动进入 SSE 实时驾驶舱。

`release-guard` 是比赛主演示，覆盖优惠、库存、支付幂等、API 和浏览器证据；
`agent-loop` 是 30 秒快速兜底。两者都会在隔离工作区创建合成案例、执行测试并生成报告，
不访问真实 TAPD、工蜂或业务仓库。最终公开包通过 EdgeOne Makers 提交，FUE 仅作内部分享或备用入口。

## 当前回放进度（2026-07-13）

8 个任务均已使用已配置模型和 `agent-strict` 真实运行，并保存为本地脱敏回放：

| 任务 | 场景 | 结论 | Agent 工具调用 | 上下文保留比例 |
| --- | --- | --- | ---: | ---: |
| `task-42` | 优惠与支付重试风险 | `fail/high` | 11 | 50.6% |
| `task-43` | 优惠修复回归 | `pass/low` | 14 | 78.1% |
| `task-45` | 大促发布守卫（默认复杂案例） | `fail/high` | 21 | 38.4% |
| `task-46` | 大促护栏修复 | `pass/low` | 16 | 55.2% |
| `task-47` | 退款权限与状态机风险 | `fail/high` | 12 | 55.9% |
| `task-48` | 退款护栏修复 | `pass/low` | 11 | 46.0% |
| `task-53` | 临时测试与敏感信息边界 | `fail/high` | 6 | 56.2% |
| `task-55` | 全栈结算异常路径 | `fail/high` | 18 | 60.1% |

每条记录均通过严格工具检查，包含隔离副本、原仓库只读、测试命令白名单、临时写入范围和禁止远端变更事件。前端选择任一 MR 后可直接播放对应回放，不再共用单一静态记录。

静态回放支持暂停、1×/2×、重新播放、跳到结论和事件进度。页面会展示真实运行模式、严格工具检查、模型自主工具数和失败驱动补测；`task-53` 还会展示现有本地安全策略真实拒绝 `git push origin main`。上下文区域准确表述为运行结束后的结构化摘要与归档压缩，不宣称已实现运行中的 context-window 压缩恢复。

长期访问链接和当前部署状态统一记录在 [`docs/部署.md`](部署.md)。

## 后续真实接入

后续可在获得授权后，通过 `mcporter-internal` 的只读 TAPD 与工蜂能力接入真实上下文：读取需求、MR 元数据和 diff，再复用现有隔离工作区、命令白名单、临时测试路径限制和公开导出脱敏流程。

真实接入默认不执行提交、推送、合并、部署或其他远程写操作；涉及真实数据和截图时，需先完成数据最小化与脱敏审查。
