# AI 测试官复现说明

本文用于复现比赛展示中的模拟 TAPD / 工蜂 MR 选择、合成测试执行和报告导出流程。所有案例均为合成数据，**不会请求真实 TAPD、工蜂或业务仓库**。

## 1. 环境准备

在仓库根目录执行：

```bash
cd /path/to/t-test-agent
uv sync --locked --extra e2e --group dev
uv run python -m playwright install chromium
```

要体验模型驱动的 `agent-strict` 流程，在本地 `.env` 配置模型凭据（不要提交）：

```bash
ARK_API_KEY=<your-key>
AI_TEST_OFFICER_MODEL=doubao-seed-2-1-turbo-260628
AI_TEST_OFFICER_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
AI_TEST_OFFICER_OPENAI_API=chat_completions
AI_TEST_OFFICER_DISABLE_TRACING=true
```

## 2. 网页任务流程

1. 在 EdgeOne Makers 控制台打开该项目的最新「预览」链接。
2. 报告首页点击 **体验模拟 TAPD / 工蜂测试任务**。
3. 从任务库选择一张 TAPD × 工蜂 MR 一一对应的合成任务卡。
4. 查看随任务即时更新的可执行测试计划，确认需求、变更、核心风险、验收标准和分层验证矩阵。
5. EdgeOne 页面可观看对应真实事件动态复盘，使用暂停、1×/2×、重新播放或跳到结论；它不会保存模型凭据或直接执行测试。
6. 本地启动任务工作台后，同一个按钮会显示为 **开始实际执行**，点击后自动进入 SSE 实时驾驶舱。

## 3. 本地实际运行

推荐直接从网页启动：

```bash
uv run ai-test-officer dashboard --runs-root runs/live-runs --host 127.0.0.1 --port 8789
```

浏览器打开 `http://127.0.0.1:8789/`，生成计划并点击执行。

例如复现优惠 / 库存 / 幂等风险：

```bash
uv run ai-test-officer demo run \
  --scenario promotion-chain \
  --demo-root runs/demos \
  --runs-root runs/reproduce \
  --run-id promotion-chain-reproduce \
  --planner-mode agent-strict \
  --allow-temp-test-code \
  --visualize \
  --dashboard-port 8789 \
  --env .env
```

命令会在 `runs/reproduce/promotion-chain-reproduce/` 创建隔离工作区、生成临时回归测试、执行验证并输出：

- `report.md`：测试结论与根因说明。
- `report.html`：可本地打开的报告。
- `run.json`：本地完整运行记录，仅供调试，不对外分享。
- `repo/tests/`：Agent 生成的临时测试，仅存在隔离工作区。

执行时会启动实时仪表盘；在浏览器打开 `http://127.0.0.1:8789/?run_id=promotion-chain-reproduce`，可看到真实命令、失败日志与最终结论。运行结束后按 `Ctrl+C` 退出本地仪表盘。

未配置模型时，可把 `--planner-mode agent-strict` 替换为 `--planner-mode deterministic`，用于离线检查报告与导出结构；该模式不展示真实 Agent 工具调用。

### 推荐默认展示案例

比赛演示建议先运行 `release-guard`：该案例会真实执行优惠边界单测、下单 API 和 Playwright 浏览器验证，并保存 `unsafe-coupon.png` 截图。它适合先向评委展示“阻断原因 + 命令 + 页面证据”的完整闭环：

```bash
uv run ai-test-officer demo run \
  --scenario release-guard \
  --demo-root runs/live-demos \
  --runs-root runs/live-runs \
  --run-id release-guard-demo \
  --planner-mode agent-strict \
  --allow-temp-test-code \
  --visualize \
  --dashboard-port 8789 \
  --env .env
```

## 4. TAPD / MR 一一对应映射

| TAPD | MR | 预期 | 本地 scenario | 核心观察点 |
| --- | --- | --- | --- | --- |
| 114514 | !42 | 阻断 | `promotion-chain` | request_id 幂等保护被移除 |
| 114515 | !43 | 通过 | `promotion-chain-pass` | 优惠策略与契约同步 |
| 114516 | !45 | 阻断 | `release-guard` | 30% 优惠边界回归 |
| 114517 | !46 | 通过 | `release-guard-pass` | 大促订单安全护栏修复 |
| 114518 | !47 | 阻断 | `refund-guard` | 退款角色校验缺失 |
| 114519 | !48 | 通过 | `refund-guard-pass` | 退款状态机守卫补齐 |
| 114520 | !53 | 阻断 | `agent-loop` | 调试令牌写入响应 |
| 114521 | !55 | 阻断 | `fullstack` | 结算页异常路径回归 |

## 5. 导出与安全检查

刷新 8 个 Agent 回放并导出比赛公开包：

```bash
npm --prefix frontend run build
uv run python scripts/build_mr_replays.py \
  --runs-root runs/mr-replays \
  --demo-root runs/mr-replay-demos \
  --dashboard-dir runs/edgeone-site/competition-dashboard \
  --env .env
uv run python -m ai_test_officer.replay_gate \
  --manifest runs/edgeone-site/competition-dashboard/replays/manifest.json \
  --runs-root runs/mr-replays \
  --public-root runs/edgeone-site/competition-dashboard
```

通过完整 replay gate 后，再按 [`部署.md`](部署.md) 部署到 EdgeOne Makers。导出包只包含脱敏事件、报告和必要证据；完整 `run.json`、本地日志和密钥不得上传或提交。

CLI 部署必须从 `public/` 目录执行，避免线上根路径落成 404：

```bash
cd runs/edgeone-site/competition-dashboard
PAGES_SOURCE=skills edgeone makers deploy \
  -n ai-test-officer-report \
  --json
```

如模型凭据、Playwright 或浏览器不可用，先使用 `--planner-mode deterministic` 验证本地导出结构；不要为了完成演示而关闭安全检查、在静态站中写入凭据，或把真实内部链接和日志直接上传。

## 6. 验证项目本身

```bash
uv run python -m unittest discover -s agents-sdk-agent/tests -p 'test_*.py' -v
npm --prefix frontend run typecheck
npm --prefix frontend run build
npm --prefix frontend run e2e

# 完整提交门禁
AI_TEST_OFFICER_REUSE_REPLAYS=1 scripts/competition_check.sh
```
