# AI 测试官作品提交材料

## 作品标题

AI 测试官：从代码变更到发布决策的自主验证 Agent

## 当前提交地址与版本

- 长期访问链接：<http://3ac366fc730e06224ec51c3d26ce88a4.makers-preview.qcdntest.net/>
- 2026-07-22 当前优化版已部署到同一 EdgeOne 项目，部署 ID 为 `dpg86xhn6onw`，包含 8 个 MR 独立动态回放、静态首页零本地 API 探测及测试计划到命令证据的定位交互。
- 长期链接当前仍是旧部署快照；开发阶段使用临时预览链接，最终优化版稳定后再申请一次重新绑定。
- 带 `eo_token` / `eo_time` 的临时预览地址不得写入 Git；只在短期验收和通知中使用。

## 作品简介（约 200 字）

AI 测试官面向需求与代码变更，先理解业务意图和风险，再自主读取 diff、选择单测、接口与浏览器验证，必要时生成临时边界测试，并基于失败日志和截图给出明确的发布结论。所有测试都在隔离副本中运行，原始业务仓库保持只读；命令、写入路径和公开产物均经过安全限制与脱敏。在线页面展示一次真实模型运行的完整回放，同时保留合成 TAPD / 工蜂任务体验。主演示通过大促订单案例识别优惠策略漂移、库存超卖和支付幂等失效，最终阻断高风险发布。

## 作品链接

- EdgeOne 项目：`ai-test-officer-report`（`makers-kyihqm91zrty`）
- 提交时从 EdgeOne Makers 控制台复制最新部署的完整预览 URL，包括访问参数。
- 不把带访问参数的临时预览 URL 提交进 Git。

## 3 分钟主演示

1. 0:00～0:20：打开 EdgeOne 首页，说明这是已完成真实运行的脱敏回放，不是网页模拟执行。
2. 0:20～0:50：展示变更意图与风险地图：优惠上限、库存和支付幂等三条风险线。
3. 0:50～1:35：点击“一键评委演示”，以 2×展示运行来源、模型读取 diff、执行命令、读取失败日志和补充边界测试。
4. 1:35～2:20：跳到结论，展示单测、API、浏览器失败证据和真实安全拦截，强调原始仓库只读、测试在隔离副本运行。
5. 2:20～2:50：回到报告结论，展示 `fail / high`、未覆盖范围和修复建议。
6. 2:50～3:00：总结价值：把“测试是否通过”升级为“是否允许发布”的可解释决策。

## 30 秒兜底

1. 打开已部署报告首页，展示 `fail / high` 与三条核心风险。
2. 点击“一键评委演示”，使用 2×或“跳到结论”快速扫过工具序列、失败驱动补测和截图证据。
3. 用一句话收尾：Agent 在隔离环境中自主验证，并以证据阻断高风险发布。

若线上主演示不可用，本地执行：

```bash
scripts/showcase_agent_loop.sh
```

## 离线回放

使用最近一次通过门禁的 `public/` 导出包。页面只读取静态、脱敏后的报告、事件和证据，不需要模型、TAPD、工蜂或业务环境在线。

## 封面建议

- 比例：16:9。
- 主标题：`AI 测试官`。
- 副标题：`让每次发布都有证据、有边界、有结论`。
- 视觉主体：左侧代码变更与 Agent 工具链，右侧红色“阻断发布”决策卡，中间用验证链路连接。
- 不使用真实业务名称、内部链接、账号、日志或截图。

## 视频备用与提交检查

- [x] 已生成 123 秒、1920×1080 主演示草稿和 27 秒兜底录屏；当前作为备用素材，不默认上传。
- [x] 视频不包含浏览器地址栏、账号、控制台信息和预览访问参数。
- [x] 电脑与 390px 手机视口分别打开最新 EdgeOne 预览链接。
- [x] 确认首页、8 个真实回放、截图证据和安全拦截均可访问。
- [x] 已生成 1600×900 封面截图。
- [ ] 官网填写标题、简介、封面和完整作品链接；仅在提交页要求时再上传视频。
- [ ] 提交后用无痕窗口复查作品链接。

本地提交素材（均位于 Git 忽略目录）：

- `runs/submission/cover-16x9.png`
- `runs/submission/main-demo-draft-1080p.webm`
- `runs/submission/fallback-demo.webm`
