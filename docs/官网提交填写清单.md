# 官网提交填写清单

> 以提交页实际字段为准。本清单默认**不上传视频**；若页面把视频标成必填，再上传本地已准备的 27 秒兜底录屏。

## 直接填写

| 提交页字段 | 填写内容 |
| --- | --- |
| 作品名称 | AI 测试官：从代码变更到发布决策的自主验证 Agent |
| 作品简介 | AI 测试官面向需求与代码变更，自主读取 diff，规划单测、接口和浏览器验证，必要时生成临时边界测试，并基于失败日志和截图给出明确发布结论。所有验证在隔离副本中完成，原仓库只读，命令和公开产物均受安全限制与脱敏保护。在线页面回放真实 Agent 的工具调用、测试计划、记忆压缩、失败驱动补测与安全拦截，让“测试通过”升级为可复核的发布决策。 |
| 作品链接 | http://48b47a0179769adfbd8ad9173d681e83.makers-preview.qcdntest.net/ |
| 源码链接（如有） | https://github.com/0awei0/t-test-agent |
| 封面（如有） | `runs/submission/cover-16x9.png`（1600×900） |
| 演示视频 | 默认留空；仅当提交页强制要求时上传 `runs/submission/fallback-demo.webm`。 |

## 提交前最后检查

- [x] 长期链接首页、回放清单和 `task-45` 回放均已验证可访问。
- [ ] 确认页面可选择 8 个 TAPD × MR 回放，默认复杂案例为 `task-45`。
- [ ] 确认首页、`replays/manifest.json` 和 `task-45` 回放都能访问。
- [ ] 上传封面；不上传视频，除非页面实际标记为必填。
- [ ] 提交后再次打开作品详情，确认作品链接没有失效。

## 备用素材

- 27 秒兜底录屏：`runs/submission/fallback-demo.webm`
- 123 秒主演示草稿：`runs/submission/main-demo-draft-1080p.webm`

这些素材都在 Git 忽略目录中；不要把 EdgeOne 临时 URL、访问参数、账号信息或本地运行产物提交到仓库。官网作品链接使用上方长期链接。
